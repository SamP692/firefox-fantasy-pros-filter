const PROJECT_ROOT = `${__dirname}/../`
const DISTRIBUTION_ROOT = `${PROJECT_ROOT}/dist`
const APPS_ROOT = `${PROJECT_ROOT}/apps`
const CONTENT_SCRIPT_ROOT = `${APPS_ROOT}/content-script`
const EXTENSION_UI_ROOT = `${APPS_ROOT}/extension-ui`

module.exports = {
    PROJECT_ROOT,
    DISTRIBUTION_ROOT,
    APPS_ROOT,
    CONTENT_SCRIPT_ROOT,
    EXTENSION_UI_ROOT
}
